// This file is currently not in use, but it can be used to fetch country data from Supabase if needed in the future.
// import { supabase } from "$lib/services/supabaseClient";

// export async function load() {
//   const { data } = await supabase.from("countries").select();
//   return {
//     countries: data ?? [],
//   };
// }
